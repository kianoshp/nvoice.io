module.exports = {
    uglify: {
      options: {
      	title: 'Build complete',  // optional
      	message: '<%= pkg.name %> build finished successfully.' //required   // Task-specific options go here.
      }
    }
  }